/* tslint:disable:no-unused-variable */
/* everything used here are no longer compatible, update later

/*
import { By }           from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject, addProviders,
  TestComponentBuilder, ComponentFixture
} from '@angular/core/testing';

import { ColumnSelectorComponent } from './column-selector.component';

describe('Component: ColumnSelector', () => {
  it('should create an instance', () => {
    let component = new ColumnSelectorComponent();
    expect(component).toBeTruthy();
  });
});

describe('Component: ColumnSelector', () => {

  let fixture: ComponentFixture<ColumnSelectorComponent>;
  let comp: ColumnSelectorComponent;
  let elem: any;

  // Setup the fixture, and provide shortcuts to the component and
  // native element.
  beforeEach((done) => {
    addProviders([TestComponentBuilder]);
    inject([TestComponentBuilder], (t) => {
      t.createAsync(ColumnSelectorComponent).then(f => {
        fixture = f;
        comp = fixture.debugElement.componentInstance;
        elem = fixture.debugElement.nativeElement;
        done(); // Signal jasmine that beforeEach is complete.
      }).catch((e) => done.fail(e));
    })(); // Call the function that inject returns.
  });

  it ('should display a button');
  it ('should display dropdown when button is clicked');
  it ('should display the correct list of columns');
  it ('should bind checkbox to column show property');
  it ('should not hide dropdown on click within dropdown');
  it ('should hide dropdown on click outside dropdown');

});*/
